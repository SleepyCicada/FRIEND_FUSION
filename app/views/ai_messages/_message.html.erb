<% if message.user.present? %>
  <div class="d-flex justify-content-end mb-3">
    <div class="p-2 px-3 rounded bg-primary text-white ai-message-bubble">
      <strong>You:</strong><br>
      <%= message.content %>
    </div>
  </div>
<% else %>
  <div class="d-flex justify-content-start mb-3">
    <div class="p-2 px-3 rounded bg-light border ai-message-bubble">
      <strong>Assistant:</strong><br>
      <%= message.content %>
    </div>
  </div>
<% end %>
